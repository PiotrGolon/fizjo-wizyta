export interface AnimatedSectionProps {
  imageSrc: string;
  altText: string;
  title: string;
  description: string;
  reverse?: boolean;
}

export interface AnimatedSectionIconProps {
  imageSrc: string;
  altText: string;
  title: string;
  description: string;
  notes?: string;
  openingHours?: string;
  preferations?: string;
  reverse?: boolean;
}
